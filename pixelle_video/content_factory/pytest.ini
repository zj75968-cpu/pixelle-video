[pytest]
# content_factory is a standalone rewrite with its own minimal venv.
# Anchoring rootdir here keeps pytest from importing the legacy
# pixelle_video/__init__.py (heavy app deps absent from this venv).
testpaths = tests
addopts = --import-mode=importlib
