# CH9329 硬件模式纯净化重构实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 移除所有非 CH9329 的设备控制代码，简化配置和架构，保留纯净的硬件串口控制方案。

**Architecture:** 删除 Phone Agent HTTP 控制和 ADB 控制相关的所有文件（约 40 个），简化配置 schema 和服务层，清理 Web UI 中的废弃功能。

**Tech Stack:** Python 3.11+, FastAPI, Streamlit, Pydantic, CH9329 串口控制

---

## 文件结构规划

### 删除的文件（40+ 个）

**Phone Agent 相关**:
- `api/routers/phone_agent.py`
- `pixelle_video/services/phone_agent_client.py`
- `pixelle_video/services/phone_agent_setup.py`
- `scripts/phone_agent.py`
- `scripts/local_agent.py`
- `scripts/setup_termux.sh`
- `scripts/install_termux_boot.sh`
- `scripts/termux_boot_start_agent.sh`
- `scripts/smoke_agent_repair.py`

**ADB 相关**:
- `check_adb.py`

**Scratch 测试脚本**（删除 30 个，保留 5 个）:
- 保留: `check_publish_err.py`, `mvp_publish.py`, `test_hardware_flow.py`, `check_vps_details.py`, `diag.sh`
- 删除: 其余 30 个测试脚本

### 修改的文件

**配置层**:
- `pixelle_video/config/schema.py` - 删除 PhoneAgentConfig、DistributionConfig、RemotePathsConfig
- `config.yaml` - 删除 phone_agent、distribution、distribution_mode 配置

**服务层**:
- `pixelle_video/services/android_device_dispatcher.py` - 简化为纯硬件模式
- `pixelle_video/services/device_manager.py` - 删除 ADB 相关方法

**API 层**:
- `api/routers/__init__.py` - 删除 phone_agent_router 引用
- `api/app.py` - 删除 phone_agent_router 注册

**Web UI**:
- `web/views/9_Settings.py` - 删除 Phone Agent 配置区域
- `web/views/4_Publish.py` - 删除 Phone Agent 设备选择
- `web/views/_post.py` - 删除 Phone Agent 发布选项

---

## Task 1: 准备工作和分支创建

**Files:**
- Create: `config.yaml.backup`
- Modify: `.git/` (创建新分支)

- [ ] **Step 1: 创建新分支**

```bash
git checkout -b refactor/ch9329-cleanup
```

Expected: `Switched to a new branch 'refactor/ch9329-cleanup'`

- [ ] **Step 2: 备份当前配置文件**

```bash
cp config.yaml config.yaml.backup
```

Expected: 备份文件创建成功

- [ ] **Step 3: 记录当前文件统计**

```bash
find . -name "*.py" -type f | wc -l
```

Expected: 显示当前 Python 文件总数（约 150 个）

- [ ] **Step 4: Commit 准备工作**

```bash
git add config.yaml.backup
git commit -m "chore: backup config before refactoring"
```

---

## Task 2: 删除 Phone Agent 相关文件

**Files:**
- Delete: `api/routers/phone_agent.py`
- Delete: `pixelle_video/services/phone_agent_client.py`
- Delete: `pixelle_video/services/phone_agent_setup.py`
- Delete: `scripts/phone_agent.py`
- Delete: `scripts/local_agent.py`
- Delete: `scripts/setup_termux.sh`
- Delete: `scripts/install_termux_boot.sh`
- Delete: `scripts/termux_boot_start_agent.sh`
- Delete: `scripts/smoke_agent_repair.py`

- [ ] **Step 1: 删除 API 路由文件**

```bash
git rm api/routers/phone_agent.py
```

Expected: `rm 'api/routers/phone_agent.py'`

- [ ] **Step 2: 删除服务层文件**

```bash
git rm pixelle_video/services/phone_agent_client.py
git rm pixelle_video/services/phone_agent_setup.py
```

Expected: 两个文件被删除

- [ ] **Step 3: 删除脚本文件**

```bash
git rm scripts/phone_agent.py
git rm scripts/local_agent.py
git rm scripts/setup_termux.sh
git rm scripts/install_termux_boot.sh
git rm scripts/termux_boot_start_agent.sh
git rm scripts/smoke_agent_repair.py
```

Expected: 6 个脚本文件被删除

- [ ] **Step 4: Commit Phone Agent 文件删除**

```bash
git commit -m "refactor: remove phone agent related files"
```

---

## Task 3: 删除 ADB 和 Scratch 测试脚本

**Files:**
- Delete: `check_adb.py`
- Delete: `scratch/` 中 30 个测试脚本

- [ ] **Step 1: 删除 ADB 文件**

```bash
git rm check_adb.py
```

Expected: `rm 'check_adb.py'`

- [ ] **Step 2: 删除 Scratch 测试脚本（保留 5 个）**

```bash
cd scratch
# 保留这 5 个文件
# check_publish_err.py, mvp_publish.py, test_hardware_flow.py, check_vps_details.py, diag.sh

# 删除其他所有文件
git rm local_publish_test.py download_and_push_cf.py download_win_cf.py
git rm test_agent_dispatch.py publish_xhs_demo.py test_comment_and_delete.py
git rm dump_detail_hierarchy.py test_dots_menu.py test_grafted_pipeline.py
git rm search_xml.py test_e2e_grafted.py view_last_lines.py
git rm check_running_processes.py clear_test_jobs.py print_queue.py
git rm check_vps_services.py test_vps_http.py test_vps_ports.py
git rm diagnose_quick.py check_registered_url.py check_agent_registration.py
git rm check_phone_processes.py vps_diagnose.py debug_phone_termux.py
git rm phone_force_wake.py setup_iqoo_agent.py deploy_and_clear.py
git rm check_vps_network.py check_vps_nginx_config.py diagnose_vps_nginx.py
git rm deploy_unbuffered.py
cd ..
```

Expected: 30 个测试脚本被删除

- [ ] **Step 3: Commit 测试脚本删除**

```bash
git commit -m "refactor: remove ADB and obsolete scratch test scripts"
```

---

## Task 4: 简化配置 Schema

**Files:**
- Modify: `pixelle_video/config/schema.py:107-129` (删除 PhoneAgentConfig)
- Modify: `pixelle_video/config/schema.py:180-186` (删除 RemotePathsConfig)
- Modify: `pixelle_video/config/schema.py:188-197` (删除 DistributionConfig)
- Modify: `pixelle_video/config/schema.py:200-220` (删除 PixelleVideoConfig 中的字段)

- [ ] **Step 1: 读取当前 schema 文件**

```bash
cat pixelle_video/config/schema.py | head -220
```

Expected: 查看当前配置结构

- [ ] **Step 2: 删除 PhoneAgentConfig 类（107-129 行）**

在 `pixelle_video/config/schema.py` 中删除:

```python
class PhoneAgentConfig(BaseModel):
    """HTTP Agent configuration for phone control without USB ADB."""
    url: str = Field(
        default="",
        description="手机 Agent 的访问地址（cloudflared 隧道 URL），如 https://xxx.trycloudflare.com",
    )
    token: str = Field(
        default="",
        description="认证 Token，与手机端 phone_agent.py --token 保持一致",
    )
    chunk_size_mb: int = Field(
        default=5,
        description="文件分块大小（MB），默认 5MB",
    )
    timeout_push: int = Field(
        default=120,
        description="单文件推送超时秒数",
    )
    agents: Dict[str, Dict[str, Any]] = Field(
        default_factory=dict,
        description="Registered phone-agent instances keyed by stable agent id.",
    )
```

- [ ] **Step 3: 删除 RemotePathsConfig 类（180-186 行）**

删除:

```python
class RemotePathsConfig(BaseModel):
    """Remote directory paths on target phone"""
    job_dir: str = Field(default="/sdcard/Tasker/jobs", description="Directory for job definition and trigger files")
    image_dir: str = Field(default="/sdcard/Pictures/TaskerUpload", description="Directory for uploading image assets")
    video_dir: str = Field(default="/sdcard/Movies/TaskerUpload", description="Directory for uploading video assets")
    screenshot_dir: str = Field(default="/sdcard/Tasker/jobs/screenshots", description="Directory for screenshots")
```

- [ ] **Step 4: 删除 DistributionConfig 类（188-197 行）**

删除:

```python
class DistributionConfig(BaseModel):
    """Android Tasker SSH distribution configuration"""
    mode: str = Field(default="legacy", description="Distribution mode: 'legacy' or 'mobile_tasker_ssh'")
    result_wait_seconds: int = Field(default=60, description="Timeout waiting for job completion")
    result_poll_interval_seconds: int = Field(default=5, description="Polling interval for result file")
    max_retry: int = Field(default=2, description="Maximum number of retries")
    batch_size: int = Field(default=3, description="Task batch size")
    send_interval_seconds: int = Field(default=2, description="Interval between sending files")
    mobile_devices_config: str = Field(default="config/devices.yaml", description="Path to devices config file")
    remote_paths: RemotePathsConfig = Field(default_factory=RemotePathsConfig, description="Paths on target device")
```

- [ ] **Step 5: 从 PixelleVideoConfig 删除字段**

在 `PixelleVideoConfig` 类中删除这些字段:

```python
distribution_mode: str = Field(default="legacy", description="Distribution mode")
phone_agent: PhoneAgentConfig = Field(default_factory=PhoneAgentConfig)
distribution: DistributionConfig = Field(default_factory=DistributionConfig)
```

- [ ] **Step 6: 验证语法正确**

```bash
python -m py_compile pixelle_video/config/schema.py
```

Expected: 无输出表示语法正确

- [ ] **Step 7: Commit 配置 schema 简化**

```bash
git add pixelle_video/config/schema.py
git commit -m "refactor: remove phone_agent and distribution config classes from schema"
```

---

## Task 5: 更新 config.yaml

**Files:**
- Modify: `config.yaml` (删除 phone_agent、distribution、distribution_mode)

- [ ] **Step 1: 删除 phone_agent 配置块**

从 `config.yaml` 删除:

```yaml
phone_agent:
  url: ''
  token: pixelle_secure_agent_token_2026
  chunk_size_mb: 5
  timeout_push: 120
```

- [ ] **Step 2: 删除 distribution_mode 字段**

删除:

```yaml
distribution_mode: legacy
```

- [ ] **Step 3: 删除 distribution 配置块**

删除:

```yaml
distribution:
  mode: phone_agent
  result_wait_seconds: 240
  result_poll_interval_seconds: 5
  max_retry: 2
  batch_size: 3
  send_interval_seconds: 2
  mobile_devices_config: config/devices.yaml
  remote_paths:
    job_dir: /sdcard/Tasker/jobs
    image_dir: /sdcard/Pictures/TaskerUpload
    video_dir: /sdcard/Movies/TaskerUpload
    screenshot_dir: /sdcard/Tasker/jobs/screenshots
```

- [ ] **Step 4: 验证 YAML 语法**

```bash
python -c "import yaml; yaml.safe_load(open('config.yaml'))"
```

Expected: 无输出表示 YAML 语法正确

- [ ] **Step 5: Commit 配置文件更新**

```bash
git add config.yaml
git commit -m "refactor: remove phone_agent and distribution config from config.yaml"
```

---

## Task 6: 简化 android_device_dispatcher.py

**Files:**
- Modify: `pixelle_video/services/android_device_dispatcher.py` (完全重写为纯硬件模式)

- [ ] **Step 1: 备份当前文件**

```bash
cp pixelle_video/services/android_device_dispatcher.py pixelle_video/services/android_device_dispatcher.py.old
```

- [ ] **Step 2: 重写为纯硬件模式**

完全替换 `pixelle_video/services/android_device_dispatcher.py` 内容:

```python
# -*- coding: utf-8 -*-
"""
硬件设备调度器（CH9329 串口控制）
"""
import asyncio
from typing import Callable
from loguru import logger
from pixelle_video.services.publish_scheduler import PublishJob
from pixelle_video.services.xhs_publisher import XHSPublisher


class DistributionAdapter:
    """
    发帖分发器（CH9329 物理硬件串口控制）。
    """
    
    @classmethod
    def get_mode(cls) -> str:
        """获取当前系统的发帖分发模式"""
        return "hardware"

    async def execute_job(
        self,
        job: PublishJob,
        progress_callback: Callable[[str], None]
    ) -> bool:
        """
        使用硬件直控模式执行发帖任务。
        
        Args:
            job: 待执行的任务实例，其中 job.serial 应为硬件 COM 口名（例如 "COM3"）
            progress_callback: 进度汇报回调
            
        Returns:
            True 执行成功，False 失败
        """
        logger.info(f"Executing job {job.job_id} using hardware mode. COM port: {job.serial}")
        
        publisher = XHSPublisher(serial=job.serial, job_id=job.job_id)
        
        if job.kind == "video":
            success = await publisher.publish_video(
                video_path=job.video_path or "",
                title=job.title,
                body=job.body,
                hashtags=job.hashtags,
                progress_callback=progress_callback,
            )
        else:
            success = await publisher.publish(
                images=job.images,
                title=job.title,
                body=job.body,
                hashtags=job.hashtags,
                progress_callback=progress_callback,
            )
            
        return success
```

- [ ] **Step 3: 验证语法**

```bash
python -m py_compile pixelle_video/services/android_device_dispatcher.py
```

Expected: 无输出表示语法正确

- [ ] **Step 4: Commit 调度器简化**

```bash
git add pixelle_video/services/android_device_dispatcher.py
git commit -m "refactor: simplify android_device_dispatcher to hardware-only mode"
```

---

## Task 7: 清理 API 路由引用

**Files:**
- Modify: `api/routers/__init__.py` (删除 phone_agent_router 导入和导出)
- Modify: `api/app.py` (删除 phone_agent_router 注册)

- [ ] **Step 1: 从 __init__.py 删除导入**

在 `api/routers/__init__.py` 中删除:

```python
from api.routers.phone_agent import router as phone_agent_router
```

- [ ] **Step 2: 从 __all__ 删除导出**

在 `api/routers/__init__.py` 的 `__all__` 列表中删除:

```python
"phone_agent_router",
```

- [ ] **Step 3: 从 app.py 删除路由注册**

在 `api/app.py` 中删除:

```python
app.include_router(phone_agent_router)
```

- [ ] **Step 4: 验证 API 启动**

```bash
python -c "from api.app import app; print('API app loaded successfully')"
```

Expected: `API app loaded successfully`

- [ ] **Step 5: Commit API 路由清理**

```bash
git add api/routers/__init__.py api/app.py
git commit -m "refactor: remove phone_agent router from API"
```

---

## Task 8: 清理 Web UI Settings 页面

**Files:**
- Modify: `web/views/9_Settings.py:159-190` (删除 Phone Agent 状态卡片)
- Modify: `web/views/9_Settings.py:252-378` (删除 Phone Agent 配置区域)

- [ ] **Step 1: 读取当前文件结构**

```bash
grep -n "Phone Agent" web/views/9_Settings.py
```

Expected: 显示所有 Phone Agent 相关行号

- [ ] **Step 2: 删除 Phone Agent 状态卡片（约 159-190 行）**

删除包含以下内容的代码块:

```python
# ── Phone Agent ──
pa_url = cfg.phone_agent.url.strip()
# ... 整个 Phone Agent 状态检查逻辑
_status_card("Phone Agent（HTTP）", pa_ok, pa_detail)
```

- [ ] **Step 3: 删除 Phone Agent 配置区域（约 252-378 行）**

删除整个配置区域:

```python
# ── 6. Phone Agent 配置 ─────────────────────────────────────────
with st.expander("📱 6. Phone Agent 配置", expanded=False):
    # ... 所有 phone_agent 配置 UI
```

- [ ] **Step 4: 验证 Streamlit 语法**

```bash
python -m py_compile web/views/9_Settings.py
```

Expected: 无输出表示语法正确

- [ ] **Step 5: Commit Settings 页面清理**

```bash
git add web/views/9_Settings.py
git commit -m "refactor: remove phone_agent config from Settings UI"
```

---

## Task 9: 清理 Web UI Publish 和 Post 页面

**Files:**
- Modify: `web/views/4_Publish.py` (删除 phone_agent 设备选择)
- Modify: `web/views/_post.py` (删除 phone_agent 发布选项)

- [ ] **Step 1: 检查 Publish 页面中的 phone_agent 引用**

```bash
grep -n "phone_agent\|Phone Agent\|termux" web/views/4_Publish.py
```

Expected: 显示所有相关引用

- [ ] **Step 2: 删除 Publish 页面中的 phone_agent 相关代码**

删除所有 phone_agent 相关的设备选择 UI 和 Termux 配置提示。

- [ ] **Step 3: 检查 Post 页面中的 phone_agent 引用**

```bash
grep -n "phone_agent\|Phone Agent" web/views/_post.py
```

Expected: 显示所有相关引用

- [ ] **Step 4: 删除 Post 页面中的 phone_agent 相关代码**

删除所有 phone_agent 相关的发布选项和设备状态显示。

- [ ] **Step 5: 验证语法**

```bash
python -m py_compile web/views/4_Publish.py
python -m py_compile web/views/_post.py
```

Expected: 无输出表示语法正确

- [ ] **Step 6: Commit Web UI 清理**

```bash
git add web/views/4_Publish.py web/views/_post.py
git commit -m "refactor: remove phone_agent references from Publish and Post UI"
```

---

## Task 10: 清理未使用的导入

**Files:**
- Modify: 所有修改过的文件（清理未使用的导入）

- [ ] **Step 1: 使用 ruff 检查未使用的导入**

```bash
ruff check --select F401 pixelle_video/services/
ruff check --select F401 api/routers/
ruff check --select F401 web/views/
```

Expected: 显示所有未使用的导入

- [ ] **Step 2: 自动修复未使用的导入**

```bash
ruff check --select F401 --fix pixelle_video/services/
ruff check --select F401 --fix api/routers/
ruff check --select F401 --fix web/views/
```

Expected: 自动删除未使用的导入

- [ ] **Step 3: Commit 导入清理**

```bash
git add -A
git commit -m "refactor: remove unused imports"
```

---

## Task 11: 验证和测试

**Files:**
- Test: 所有修改的文件

- [ ] **Step 1: 验证配置加载**

```bash
python -c "from pixelle_video.config import config_manager; config_manager.reload(); print('Config loaded successfully')"
```

Expected: `Config loaded successfully`

- [ ] **Step 2: 验证 API 启动**

```bash
python -c "from api.app import app; print('API app loaded successfully')"
```

Expected: `API app loaded successfully`

- [ ] **Step 3: 检查文件统计**

```bash
find . -name "*.py" -type f | wc -l
```

Expected: 显示减少后的文件数（约 110 个，减少 27%）

- [ ] **Step 4: 运行代码质量检查**

```bash
ruff check pixelle_video/ api/ web/
```

Expected: 无严重错误

- [ ] **Step 5: Commit 验证结果**

```bash
git add -A
git commit -m "test: verify refactoring changes"
```

---

## Task 12: 更新文档

**Files:**
- Modify: `README.md` (更新配置说明)
- Create: `docs/MIGRATION.md` (迁移指南)

- [ ] **Step 1: 更新 README.md 配置部分**

在 README.md 中更新配置说明，删除所有 Phone Agent 和 distribution 相关的配置说明。

- [ ] **Step 2: 创建迁移指南**

创建 `docs/MIGRATION.md`:

```markdown
# 配置迁移指南

## 从 Phone Agent 模式迁移到纯硬件模式

### 自动迁移

配置加载器会自动忽略以下废弃字段：
- `phone_agent`
- `distribution`
- `distribution_mode`

### 手动清理（可选）

如果您想手动清理配置文件，删除以下配置块：

```yaml
# 删除这些配置
phone_agent:
  url: ''
  token: ''
  
distribution_mode: legacy

distribution:
  mode: phone_agent
```

### 设备数据迁移

如果您之前使用 Phone Agent 设备，这些设备将不再可用。
请重新注册硬件设备（COM 端口）。

### 恢复 Phone Agent（如需要）

如果未来需要恢复 Phone Agent 功能：

```bash
git log --all --full-history -- "*phone_agent*"
git checkout <commit-hash> -- pixelle_video/services/phone_agent_client.py
```
```

- [ ] **Step 3: Commit 文档更新**

```bash
git add README.md docs/MIGRATION.md
git commit -m "docs: update README and add migration guide"
```

---

## Task 13: 最终验证和合并

**Files:**
- Test: 整个项目

- [ ] **Step 1: 运行完整的代码检查**

```bash
ruff check .
```

Expected: 无严重错误

- [ ] **Step 2: 验证 Web UI 启动**

```bash
# 在后台启动 Streamlit（测试用）
timeout 10 streamlit run web/app.py --server.headless true || true
```

Expected: Streamlit 能够启动（10 秒后自动停止）

- [ ] **Step 3: 查看所有提交**

```bash
git log --oneline refactor/ch9329-cleanup ^main
```

Expected: 显示所有重构相关的提交

- [ ] **Step 4: 创建最终总结提交**

```bash
git add -A
git commit -m "refactor: complete CH9329 hardware mode cleanup

- Removed 40+ files (Phone Agent, ADB, obsolete test scripts)
- Simplified config schema (removed PhoneAgentConfig, DistributionConfig)
- Simplified service layer (hardware-only dispatcher)
- Cleaned up API routes and Web UI
- Updated documentation

Code reduction: ~27% (from 150 to 110 Python files)
Config simplification: ~38% (from 96 to 60 lines)"
```

- [ ] **Step 5: 推送分支（可选）**

```bash
git push origin refactor/ch9329-cleanup
```

Expected: 分支推送成功

---

## 验证清单

完成所有任务后，验证以下项目：

- [ ] 配置文件加载无错误
- [ ] API 服务可以启动
- [ ] Web UI 可以启动
- [ ] Settings 页面不显示 Phone Agent 配置
- [ ] 设备管理功能正常
- [ ] 代码库减少约 27%（从 ~150 个文件降至 ~110 个）
- [ ] 无未使用的导入
- [ ] 文档已更新

## 回滚计划

如果需要回滚：

```bash
# 回滚到 main 分支
git checkout main
git branch -D refactor/ch9329-cleanup

# 或者从特定提交恢复文件
git checkout <commit-hash> -- <file-path>
```

---

**预计总时间**: 约 2-3 小时（实际执行时间可能更短，因为是删除操作为主）

**风险等级**: 低（主要是删除操作，可随时从 Git 历史恢复）
